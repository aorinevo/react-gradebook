import expect from 'expect';

describe('Initial setup', ()=>{
  it('should pass in first test', () => {
    expect(1).toBe(1);
  });
  // it('should fail in second test', () => {
  //   expect(1).toBe(0);
  // });
});